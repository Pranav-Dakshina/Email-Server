<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title><%=title%></title>
  <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.6.4/angular.min.js"></script>
  <link rel="stylesheet" type="text/css" href="styles.css">
</head>

<body ng-app="myApp">
  <div class="thabpet">
    Thabpet
  </div>
  <form class="form form-content" ng-controller="signInController" name="signInForm" novalidate>
    <input class="inp" type="text"  name="user" ng-model="user" placeholder="Username" required>
    <div class="err-msg">
    <span ng-show="signInForm.user.$dirty && signInForm.user.$invalid">
      <span ng-show="signInForm.user.$error.required">Username is required</span>
    </span>
  </div>
    <br>
    <input class="inp" type="password" name="pass" ng-model="pass" placeholder="Password" required>
    <div class="err-msg">
    <span ng-show="signInForm.pass.$dirty && signInForm.pass.$invalid">
      <span ng-show="signInForm.pass.$error.required">Password is required</span>
    </span>
  </div>
    <input class="submit" type="submit" name="submit" ng-disabled="signInForm.user.$dirty && signInForm.user.$invalid || signInForm.pass.$dirty && signInForm.pass.$invalid">
  </form>
</body>
  <script src="signin.js"></script>
</html>
